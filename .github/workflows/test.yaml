name: Testing descriptions checker

on:
  pull_request:
    branches:
      - main

permissions:
  pull-requests: write

jobs:
  passing-test:
    runs-on: ubuntu-latest
    outputs:
      exitcode: ${{ steps.test-passing.outputs.exitcode }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        id: test-passing
        continue-on-error: false
        with:
          path: "test/passing"

  failing-test:
    runs-on: ubuntu-latest
    outputs:
      exitcode: ${{ steps.test-failing.outputs.exitcode }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        id: test-failing
        continue-on-error: true
        with:
          path: "test/failing"

  report-to-pr:
    runs-on: ubuntu-latest
    needs:
      - passing-test
      - failing-test
    steps:
      - uses: marocchino/sticky-pull-request-comment@v2
        with:
          recreate: true
          message: |
            Test _pass_ exited with code **${{ needs.passing-test.outputs.exitcode }}**
            Test _fail_ exited with code **${{ needs.failing-test.outputs.exitcode }}**